#include<iostream>
using namespace std;
int main(){
    int a,b;
    cout<<"Enter the first num: ";
    cin>>a;
    cout<<"Enter the second num: ";
    cin>>b;
    if(a > b ) cout<<"The largest num is: "<< a;
    else cout<<"The largest num is: "<< b;
    
}